# Optional frontend overrides for local development

# API base URL used by frontend in dev mode.
# Defaults to http://localhost:3001/api when unset.
VITE_API_URL=http://localhost:3001/api

# Optional base path when serving frontend from a subpath.
# Keep '/' for normal local development.
VITE_BASE_PATH=/
